int cliff, edge = 0; //infrared detectable obstacles
int distance = 0; //distance to obstacle
int min_distance = 10; //Minimum distance to an obstacle
int lab = 1; //Robot at lab

void setup() {
  
}

void loop() {
  if(cliff == 1){
    
  }
  
  if(edge == 1){
  }
  
  if(distance < min_distance){
  }
}
